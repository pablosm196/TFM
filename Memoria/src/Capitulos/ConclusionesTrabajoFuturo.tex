\chapter{Conclusiones y Trabajo Futuro}
\label{cap:conclusiones}

El objetivo de este estudio consistía en la creación de una herramienta que, dadas unas tareas y unos condicionales, fuese capaz de generar árboles de comportamiento para personajes no jugador que sirviesen como base para rebajar la carga de trabajo de los diseñadores de videojuegos.

Dados los resultados anteriormente mencionados se han llegado a las siguientes conclusiones.

\section{Conclusiones}

Aunque de forma preliminar debido a la escasez de usuarios encontrados para las pruebas de usuario, se ha llegado a la conclusión de que este modelo es capaz de generar árboles de comportamiento funcionales en un tiempo inferior al de un humano, consiguiendo así el objetivo general planteado.
Además se ha conseguido implementar un sistema de validación automático que permite reforzar el sistema y que el diseñador pueda observar en tiempo real la ejecución del comportamiento diseñado.

% El objetivo de este estudio consistía en la creación de una herramienta que, dadas unas tareas y condicionales, fuese capaz de generar \glspl{BT} que sirviese como base para diseñadores de videojuegos, aliviando así la carga de trabajo de éstos teniendo una base sobre la que trabajar.
% Para ello se escogió el motor Unreal Engine 5, donde se crearon varios escenarios así como una serie de tareas y condicionales para poder probar las generaciones de los \glspl{BT}.
% Con la base creada se continuó con el modelo de la herramienta, en la que se decidió el flujo de ejecución y generación.
% Se procedió con la implementación del generador de \glspl{BT}, en la que se usaron dos \glspl{LLM} para las tareas de generación de pseudocódigo y selección de variables de \gls{BB} para las tareas y decoradores que lo necesitasen.
% Esta implementación consistió en el uso de técnicas de \textit{prompt engineering} con el fin de disminuir las alucinaciones de los modelos.
% A su vez se implementó un sistema de \gls{RAG} para poder dotar al modelo de ejemplos útiles.
% Con este fin se buscó un dataset lo suficientemente extenso como para que el sistema fuese útil, por lo que se decidió usar el dataset publicado en \cite{llmbrain}.
% Este dataset ha sido limpiado y traducido al pseudocódigo usado en la generación.
% Una vez se tenía la generación implementada se hizo un sistema de validación de los \glspl{BT} para verificar que cumplen los objetivos propuestos y la dependencia de las acciones.
% Para las dependencias se implementó un NFA, y para los objetivos una clase auxiliar que comprobase las condiciones necesarias al finalizar la ejecución.
% Junto a esto se implementó un sistema en el que, si falla alguna de las validaciones, se le pide a la herramienta que arregle el pseudocódigo dado el fallo que haya tenido el anterior.
% Con el fin de validar experimentalmente los resultados se diseñaron unas pruebas de usuarios en los que se comprobaba la usabilidad y la funcionalidad de la herramienta.

% Las conclusiones a las que se han llegado de diferentes partes del trabajo se detallan en las siguientes secciones.

% \subsection{Conclusiones de los escenarios implementados}
% Debido al fin del trabajo de desarrollar una herramienta que sirva como apoyo para diseñadores de videojuegos, se implementaron diferentes escenarios en un proyecto de Unreal Engine 5.
% Los escenarios, tareas y condicionales implementados han cumplido su función como marco en el que probar la generación de los \glspl{BT}, consiguiendo un marco en los que los diseñadores de videojuegos se sientan acostumbrados y se consiga la opacidad buscada de la herramienta frente a estos.

% \subsection{Conclusiones de la herramienta generadora de \glspl{BT}}
% Tras los intentos mencionados en la sección \ref{sec:GeneracionBTs}, se ha concluido que la forma de generar \glspl{BT} que mejores resultados ha dado ha consistido en convertir las peticiones y las tareas y decoradores disponibles en un formato de pseudocódigo que luego ha podido ser traducido y completado con el uso de variables de la \gls{BB} gracias a un segundo modelo \gls{LLM}.

% Para la generación de pseudocódigo se ha necesitado del modelo ``mistral-large-3'', con un total de 675B de parámetros.
% La ventaja de esto es que la generación resulta bastante correcta, tanto en la calidad de las generaciones como en el formato resultante, ya que modelos más pequeños no conseguían generaciones en las que se cumpliesen los objetivos o las dependencias y el formato del pseudocódigo pedido solía no cumplir con la sintaxis especificada.
% Otra ventaja de usar un modelo grande mediante \gls{api} es el poco tiempo que consigue generar el pseudocódigo, a diferencia de otros modelos en local que tendrían una latencia mayor.
% Sin embargo, la dependencia de \glspl{api} externas puede traer problemas a largo plazo, ya que pueden actualizar los términos de uso o el listado de precios o directamente cerrar el modelo.
% Es por esto último que se estima necesario el estudio del uso de modelos locales mediante técnicas de \gls{Fine-tuning} para verificar un acercamiento de los resultados observados sin las desventajas mencionadas siempre y cuando el tiempo de generación no aumente de forma significativa.

% Para la asignación de variables de \gls{BB} se ha utilizado el modelo local ``llama3:8b'', que cuenta con 8B de parámetros.
% Esto se decidió así debido a las peticiones limitadas a la \gls{api} de Mistral.
% El hecho de ser un modelo más pequeño que el anterior ha resultado en algunos fallos a la hora de fijar las variables, siendo un motivo por el que los usuarios tengan que revisar y cambiar los JSONs generados.
% Sin embargo estos fallos no suelen ser habituales, por lo que dadas las pruebas de validación se puede concluir que cumple con su cometido.
% A pesar de no ser habituales estos fallos se considera igualmente la investigación del uso de otro modelo más grande y la búsqueda de datos para poder realizar y comparar un \gls{Fine-tuning} con el uso de un sistema \gls{RAG} para este modelo con el fin de reducir las alucinaciones y aumentar la precisión, lo que implicaría disminuir la intervención humana y reducir aún más la carga de trabajo de los diseñadores.

% Para el sistema \gls{RAG} montado se buscaron datasets de \glspl{BT} específicos de videojuegos, pero la ausencia de este tipo de datos ha implicado que se haya usado un dataset de \glspl{BT} de robótica para posteriormente traducirlos al formato buscado para el trabajo.
% A pesar de ser un ámbito distinto estos ejemplos han funcionado para la generación de pseudocódigo, ya que las tareas de robótica incluidas en el dataset se pueden asemejar a las tareas que debe realizar un \gls{npc}, consiguiendo unos \glspl{BT} con mayor precisión (menor número de nodos adicionales o selección de nodos de composición más certera).
% Un aspecto negativo de este sistema es que el tiempo de generación aumentaba en las ocasiones en las que no estaba guardado en memoria, retrasando la generación unos cuantos segundos.
% Para disminuir las alucinaciones también se ha hecho uso de varias técicas de \textit{prompt engineering}, tales como ``Few-shot prompting'' o ``Chain-of-Thought'', consiguiendo así \glspl{BT} que logran cumplir con los objetivos propuestos para los comportamientos pedidos.
% Una técnica fundamental ha sido ``Self-Critique Prompting'', ya que en varios casos ha logrado corregir errores de alucinaciones en cuanto a que el modelo se inventaba tareas, decoradores o la sintaxis del pseudocódigo.

% Con todo esto se puede concluir que, aún no conseguir resultados completamente perfectos, la herramienta es capaz de generar \glspl{BT} que sirvan como una buena base para generar comportamientos en los que los diseñadores solo tengan que cambiar unos pocos detalles para lograr el objetivo final, reduciendo así su carga de trabajo.

% \subsection{Conclusiones de la validación automática}
% Para poder validar y corregir cuando es debido de forma automática los \glspl{BT} generados se han propuesto dos tipos de validaciones.
% Estas validaciones no son obligatorias de usar en la herramienta, ya que pueden suponer un aumento de carga de trabajo para los diseñadores que no compensa el trabajo ahorrado de generar los \glspl{BT} a mano.
% Sin embargo esta herramienta puede ayudar a la automatización del proceso con el arreglo de fallos de forma automática.

% La primera validación realizada consiste en la verificación del cumplimiento de dependencias de las tareas.
% Debido a la probable duplicación de tareas en diferentes puntos del \gls{BT} se ha optado por la implementación de un NFA.
% Debido a que no se ha encontrado la forma de poder verificar en cada tick qué nodos está ejecutando un \gls{BT} de Unreal Engine, se han tenido que hacer de forma explícita las llamadas al NFA desde las tareas para indicar cuál se está ejecutando.
% A pesar de este aspecto negativo, el NFA ha resultado cumplir su función de forma correcta, consiguiendo que la herramienta corrija los fallos de dependencias en el pseudocódigo con la información que se le transmite (pseudocódigo fallido, acción en la que ha fallado y una lista con todas las acciones ejecutadas previamente).

% La segunda validación consiste en un sistema para validar los objetivos de los comportamientos buscados.
% El sistema consiste en un componente de actor al que se llama al finalizar la ejecución, el cual si falla la verificación devuelve un mensaje de error y llama de nuevo a la herramienta.
% Este método ha conseguido que, en los casos en los que los \glspl{BT} resultaban fallidos, la herramienta sea capaz de comprender mejor la acción buscada y reestructurar el \gls{BT} para conseguir uno correcto.

% Para esta validación se probaron tres escenarios en los que se probaba una generación sencilla sin apenas dependencias, otra sencilla en la que había más dependencias de tareas y una última con varias varaibles de \gls{BB} y un comportamiento más complejo.
% Para estos tres escenarios se consiguieron unas generaciones válidas tanto en objetivos como en dependencias.

% \subsection{Conclusiones de las pruebas de usuarios}
% Como bien se ha comentado en la sección \ref{sec:Usuarios} estas pruebas se han realizado con un número reducido de usuarios (N=3), por lo que las conclusiones no se deben tomar como definitivas sino como una aproximación.

% Teniendo en cuenta el objetivo final de este trabajo se formularon cuatro hipótesis para las pruebas de usuario:

% \begin{enumerate}
%     \item \texttt{H1}: La herramienta es más rápida que un humano
%     \item \texttt{H2}: La generación de los \glspl{BT} generados es similar a los realizados a mano
%     \item \texttt{H3}: La calidad de los \glspl{BT} generados es ligeramente menor pero sirve como punto de partida.
%     \item \texttt{H4}: La herramienta es intuitiva y fácil de usar.
% \end{enumerate}

% Tras las pruebas de usuario y una siguiente encuesta se han llegado a las siguientes conclusiones sobre cada hipótesis:

% \begin{enumerate}
%     \item \texttt{H1}: Según la observación experimental los usuarios tardaban más en realizar los \glspl{BT} a mano que en generarlos con la herramienta.
%     \item \texttt{H2}: La herramienta generaba \glspl{BT} que conseguían una ejecución similar a los realizados a mano
%     \item \texttt{H3}: Los \glspl{BT} generados colocaban nodos extra (tanto nodos de composición como tareas) o no lograban asignar correctamente todas las variables de \gls{BB} a las tareas, haciendo que los \glspl{BT} generados fuesen de una calidad inferior a los humanos, aunque formaba una base para que la intervención humana sea solo necesaria en cambiar esos aspectos.
%     \item \texttt{H4}: La encuesta SUS realizada para medir la usabilidad dio un resultado de 90.83/100, por lo que se confirma la facilidad de uso de la herramienta para diseñadores.
% \end{enumerate}

% Con estas pruebas se puede ver que se cumplen las cuatro hipótesis pensadas inicialmente, aunque se necesitan más usuarios que pidan el perfil pedido (diseñadores de videojuegos que haya trabajado con Unreal Engine y su sistema de \glspl{BT}) para poder cerrar las conclusiones de forma definitiva.


\section{Trabajo Futuro}
Dadas las limitaciones planteadas en la sección \ref{sec:Limitaciones}, el trabajo a futuro de este estudio se puede centrar en varios esfuerzos:
\begin{itemize}
    \item Con capacidades computacionales mayores estudiar el uso de modelos locales de mayor tamaño para los modelos usados, así como el uso de técnicas de Fine-tuning para comprobar si su uso alcanzaría o mejoraría los resultados actuales con el fin de no depender de APIs de terceros.
    \item Mejorar el sistema RAG con BTs de videojuegos y, adicionalmente, que se guarden en disco el almacén vectorial y se puedan buscar por metadatos gracias al uso de Qdrant, suponiendo así una posible mejora en el contexto dado a los modelos y reduciendo la latencia.
    \item Implementar un agente LLM con una interfaz visual para cambiar los BTs generados de forma más sencilla y que se le pueda dar feedback al generador mediante uso de lenguaje natural.
    \item Implementar un agente LLM que pueda leer las acciones y descripciones de los proyectos para automatizar su suscripción a la Abstract Factory y su descripción para el paso a la herramienta.
    \item Implementar un agente LLM que pueda automatizar las pruebas de validación generando el órden de dependencias y una funciñon de objetivos.
    \item Recopilar más usuarios para las pruebas con el fin de conseguir un número estadísticamente significativo para concluir definitivamente las hipótesis propuestas.
\end{itemize}