{
    "prompt": "Write a behavior tree for the robot to execute the command using only available nodes. If the object is close, grab it, move to processing station, process it, otherwise move to next object. list of available nodes: <Action ID = GrabObject    /> <Action ID = ProcessObject    /> <Action ID = MoveToProcessingStation    /> <Action ID = MoveToNextObject    /> <Action ID = ApproachObject    /> <Action ID = ScanAreaForObject    /> <Condition ID = IsObjectVisible    /> <Condition ID = IsObjectApproached    /> <Condition ID = IsObjectInGripper    /> <SubTree ID = ProcessObject    />",
    "BT": {
        "Blackboard": [
            {
                "IsObjectApproached": "Boolean"
            }
        ],
        "Node": {
            "Type": "Sequence",
            "Decorators": [],
            "Nodes": [
                {
                    "Type": "Task",
                    "Decorators": [
                        {
                            "IsObjectApproached?": "IsObjectApproached"
                        }
                    ],
                    "Nodes": [],
                    "Task": "GrabObject",
                    "Name": "GrabObject"
                }
            ]
        }
    }
}