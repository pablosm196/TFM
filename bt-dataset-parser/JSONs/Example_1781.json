{
    "prompt": "Write a behavior tree for the robot to execute the command using only available nodes.     If object is visible, move to and grab it. Check properties. If not, move to destination or scan area.     list of available nodes: <Action ID = MoveToDestination    /> <Action ID = ScanArea    /> <Action ID = TakeObject    /> <Action ID = GrabObject    /> <Action ID = MoveToObject    /> <SubTree ID = ProcessObjectProperties    /> <Condition ID = IsObjectGrabbable    /> <Condition ID = IsObjectVisible    /> <Condition ID = IsObjectApproached    /> <Condition ID = IsPathClear    />",
    "BT": {
        "Blackboard": [
            {
                "IsObjectApproached": "Boolean"
            }
        ],
        "Node": {
            "Type": "Sequence",
            "Decorators": [],
            "Nodes": [
                {
                    "Type": "Task",
                    "Decorators": [
                        {
                            "IsObjectApproached?": "IsObjectApproached"
                        }
                    ],
                    "Nodes": [],
                    "Task": "TakeObject",
                    "Name": "TakeObject"
                }
            ]
        }
    }
}